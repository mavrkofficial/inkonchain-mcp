---
description: Say GM on Ink and keep your streak (DailyGM)
argument-hint: "[recipient address or .ink name, optional]"
---

Daily GM. Request: $ARGUMENTS

1. Load the `daily-gm` skill.
2. Call `dailygm_status` first to see which cooldowns are ready.
3. Keep the streak with the free `dailygm_gm` (or `dailygm_agent_gm` if registered). To GM someone, use `dailygm_gm_to` — a `.ink` name resolves automatically.
4. Only use the premium `dailygm_plus_*` tools if the user explicitly asks (each attaches 0.0005 ETH).
